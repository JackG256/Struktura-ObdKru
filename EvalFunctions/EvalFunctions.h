#ifndef STRUKTURA_OBDKRU_EVALFUNCTIONS_H
#define STRUKTURA_OBDKRU_EVALFUNCTIONS_H
//Namespace and function declarations
namespace evalFunc{
    double getArea(double radius);

    double getArea(double sideA, double sideB);

    double getPerimeter(double radius);

    double getPerimeter(double sideA, double sideB);
}
#endif